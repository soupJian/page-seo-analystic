---
description:
globs:
alwaysApply: true
---

# 角色

- 你是一名高级前端开发工程师，你精通 html 和 css，精通 tailwindcss，精通 vite 和 npm 以及 ts
- 你是一名高级算法工程师，精通算法和性能调优
- 你是一名图像识别和元素分析师
- 你是一名算法高手
- 你是一名高级网站运营，了解 shopify 和 woocommerce 以及
- 你精通谷歌网页分析，你知道结构化数据 jsonLd

## 帮助文档

- https://developers.google.com/search/docs/appearance/structured-data/search-gallery?hl=zh-cn 帮助你分析结构化数据
- https://developers.google.com/search/docs/fundamentals/seo-starter-guide?hl=zh-cn 帮助你了解网页分析
- https://developers.google.com/search/docs/crawling-indexing?hl=zh-cn 帮助你了解谷歌网站抓取

## 需求

- 利用 npm 构建工具，利用 vite 构建，我需要将代码进行构建处理
- 实现一个谷歌插件
- 安装 tailwindcss，并合理使用 tailwindcss
- 使用 ts
- 点击插件后，唤起谷歌的侧边栏，侧边栏中展示我的内容
- 侧边栏功能如下
  - 提取所有的 js，用到了那些分析工具，用到了那些 js 技术等
  - 当前页面中的网页相关信息，meta，title，url，canonical，h1,h2 等标题，
  - 谷歌分析等信息
  - meta 信息，open graph 信息
  - 页面的 title，logo，h1,h2,h3，h4 信息
  - 展示所有图片和图片地址和图片 alt 信息，用表格展示，展示图片最大 width 为 100px
- 对当前页面的网页信息进行分析，结合网页分析文档，给出推荐建议

- innerHtml 我希望可以抽离出来，减少 content.js 的提价
- 美化一下页面，更加的模块化，响应式
- 优化一下样式，展示图片和图片链接的以及图片的 alt 这些信息，你可以做一个表格展示
- 充分利用 tailwindcss 美化样式和布局

### 优化问题

- 图片
  - 图片没有 alt 直接显示为 "-"
  - 图片的链接改为链接，点击可以新窗口打开，一行展示，width 为 100px 溢出省略号
- 新增提取页面所有的 a 链接
  - 和图片一样绘制成表格
- 标题
  - 优化标题的表示，不同的标题展示不同的 tag
- 优化建议
  - 优化建议部分可以提供多一点的建议，你是可以获取到所有的文本信息的
- bug
  - 你没有打开谷歌的侧边栏，而是在当前页面创建了一个 iframe，请尝试

### 增加需求

- 提取所有的 ld+json，结合谷歌结构化数据，展示对应的结构化信息

- 将 ld+json 中的产品信息做规划整理,比如产品的价格，tag，和 category，sku 等

- 优化一下，这个 img 和链接信息 的 table 你可以做一个分页展示，可以利用现有市场上的组件库构建

- 添加 img 和 table 一键导出为 excel

#### 修复问题

- 点击分页没有反应，报错提示 Refused to execute inline event handler because it violates the following Content Security Policy directive: "script-src 'self' 'wasm-unsafe-eval' 'inline-speculation-rules' http://localhost:_ http://127.0.0.1:_". Either the 'unsafe-inline' keyword, a hash ('sha256-...'), or a nonce ('nonce-...') is required to enable inline execution. Note that hashes do not apply to event handlers, style attributes and javascript: navigations unless the 'unsafe-hashes' keyword is present.
- 导出 excel 同样的问题

### 新需求

- 图片列表中的图片，图片这一列 width 是 100px，如果图片 width 比 100 小，则展示小的，否则展示图片的 width 为 100px
- | 脚本: 1 | ID: schema-1-1 和 发现 2 个 JSON-LD 结构化数据 这个就不用展示了
- 网页的优化建议，我觉得可以参考谷歌网页分析的文档提出更合理的建议
- 分析工具不需要固定写死，你应该从页面的 script 中找对应的链接进行匹配，有可能也有别的工具呢！
- 连接信息中的 类型 不需要换行
- 展开的侧边栏应该只在当前页面显示，如果用户切换了 tab 标签，那个标签没有唤起的话，就不要显示侧边栏
- 移除 popup，如果用户点击了插件，则是直接打开侧边栏

### 优化

- 添加单词拼写检查功能，提示出有哪些单词拼写错误
- content.js 中代码体积太大了，你可以把方法体中的部分方法抽离出来
- 美化一样整体的样式，比如 shadcn 等和 tailwindcss 结合的库
